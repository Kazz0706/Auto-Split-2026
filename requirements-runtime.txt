# Raspberry Pi 5用の分割推論実行環境（Python 3.11 / linux-aarch64）。
# 直接利用するパッケージだけを固定する。推移依存はpipに解決させ、
# ビルド成功後に `pip freeze` でrequirements-runtime.lockへ記録する。
torch==2.9.0
torchvision==0.24.0
ultralytics==8.3.222
numpy==2.2.6
opencv-python==4.12.0.88
Pillow==12.0.0
PyYAML==6.0
